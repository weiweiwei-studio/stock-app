# 資料完整性規則上線順序

這一版為 `stock_items` 加入 `_version`，並要求每次更新只能遞增一個版本，避免舊畫面覆蓋較新的資料。

## 必須依照此順序

1. 合併本 PR，等待 GitHub Pages 更新。
2. 重新整理所有正在使用 Stock App 的瀏覽器分頁。
3. 測試一次地點調動、商品儲存與售出狀態。
4. 確認新版正常後，才將本 PR 的 `firestore.rules` 發布到 Firebase Console。
5. 規則發布後，再測試一次修改功能。

不要在新版網站上線前先發布規則；舊版程式不會遞增 `_version`，會被新版規則拒絕寫入。
