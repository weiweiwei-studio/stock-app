{
  "name": "weiweiwei-stock-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind-input.css -o ./styles.css --minify",
    "check:css": "npm run build:css && git diff --exit-code -- styles.css",
    "test": "node --test view-utils.test.mjs auth-utils.test.mjs data-integrity.test.mjs security-utils.test.mjs image-utils.test.mjs image-migration-utils.test.mjs",
    "test:mobile": "playwright test",
    "check": "npm test && npm run check:css && npm run test:mobile"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "tailwindcss": "3.4.17"
  }
}
